server:
  port: 8989

management:
  endpoints:
    web:
      exposure:
        include: *

zuul:
  routes:
    compras:
      service-id: Compras-Servicios
      path: /usuarios
    usuarios:
      service-id: Usuarios-Servicios
      path: /compras
